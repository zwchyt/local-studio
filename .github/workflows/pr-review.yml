name: CodeRabbit

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'controller/**'
      - 'cli/**'
      - 'frontend/**'
      - 'tests/**'

permissions:
  pull-requests: read
  contents: read
  issues: read

jobs:
  coderabbit:
    name: CodeRabbit PR Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: CodeRabbit placeholder
        run: echo "CodeRabbit action repo unavailable; skipping automated review."
